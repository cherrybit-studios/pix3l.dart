// The Pix3l class can only be constructed via Pix3l.init(), which requires a
// browser environment with the PIX3L JS SDK loaded. Integration tests covering
// the full SDK lifecycle should be run against a real browser.
//
// Unit tests for the individual Dart model types are located in
// test/src/models/.

void main() {}
